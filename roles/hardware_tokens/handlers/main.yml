---
# =============================================================================
# Hardware Tokens Handlers
# =============================================================================

- name: Hardware tokens reload udev
  ansible.builtin.shell:
    cmd: 'udevadm control --reload-rules && udevadm trigger'
  changed_when: false
