---
- name: Service | Manage pcscd socket
  ansible.builtin.systemd_service:
    name: '{{ __hardware_tokens_pcscd_service }}'
    enabled: "{{ hardware_tokens_pcscd_socket_enabled | bool }}"
    state: "{{ hardware_tokens_pcscd_socket_enabled | bool | ternary('started', 'stopped') }}"
