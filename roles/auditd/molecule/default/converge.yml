---
- name: Converge
  hosts: all
  gather_facts: true

  tasks:
    - name: Converge | Include auditd role  # noqa: role-name[path]
      ansible.builtin.include_role:
        name: '{{ playbook_dir }}/../..'
