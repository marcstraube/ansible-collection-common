---
# =============================================================================
# DH Parameters Tasks
# =============================================================================

- name: DHParam | Generate DH parameters
  community.crypto.openssl_dhparam:
    path: '{{ pki_dhparam_path }}'
    size: '{{ pki_dhparam_size }}'
    owner: root
    group: root
    mode: '0644'
