{{ ansible_managed | comment }}
#
# OpenSSL CA Configuration

[ ca ]
default_ca = CA_default

[ CA_default ]
dir               = {{ pki_ca_dir }}
certs             = $dir/certs
crl_dir           = $dir/crl
new_certs_dir     = $dir/newcerts
database          = $dir/index.txt
serial            = $dir/serial

private_key       = $dir/private/ca.key
certificate       = $dir/certs/ca.crt

crlnumber         = $dir/crlnumber
crl               = $dir/crl/ca.crl
crl_extensions    = crl_ext
default_crl_days  = {{ pki_crl_validity_days }}

default_md        = {{ pki_default_md }}

name_opt          = ca_default
cert_opt          = ca_default
default_days      = {{ pki_server_validity_days }}
preserve          = no
policy            = policy_match

[ policy_match ]
{% for field, requirement in pki_policy_match.items() %}
{{ field }} = {{ requirement }}
{% endfor %}

[ req ]
default_bits        = {{ pki_ca_key_size }}
distinguished_name  = req_distinguished_name
string_mask         = utf8only
default_md          = {{ pki_default_md }}
x509_extensions     = v3_ca

[ req_distinguished_name ]
countryName                     = Country Name (2 letter code)
countryName_default             = {{ pki_ca_country }}
stateOrProvinceName             = State or Province Name
stateOrProvinceName_default     = {{ pki_ca_state }}
localityName                    = Locality Name
localityName_default            = {{ pki_ca_locality }}
organizationName                = Organization Name
organizationName_default        = {{ pki_ca_organization }}
organizationalUnitName          = Organizational Unit Name
organizationalUnitName_default  = {{ pki_ca_organizational_unit }}
commonName                      = Common Name
commonName_default              = {{ pki_ca_common_name }}
emailAddress                    = Email Address
emailAddress_default            = {{ pki_ca_email }}

[ v3_ca ]
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid:always,issuer
basicConstraints = critical, CA:true
keyUsage = critical, digitalSignature, cRLSign, keyCertSign

[ v3_intermediate_ca ]
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid:always,issuer
basicConstraints = critical, CA:true, pathlen:0
keyUsage = critical, digitalSignature, cRLSign, keyCertSign

[ usr_cert ]
basicConstraints = CA:FALSE
nsCertType = client, email
nsComment = "OpenSSL Generated Client Certificate"
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid,issuer
keyUsage = critical, nonRepudiation, digitalSignature, keyEncipherment
extendedKeyUsage = clientAuth, emailProtection

[ server_cert ]
basicConstraints = CA:FALSE
nsCertType = server
nsComment = "OpenSSL Generated Server Certificate"
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid,issuer:always
keyUsage = critical, digitalSignature, keyEncipherment
extendedKeyUsage = serverAuth
{% if pki_ocsp_url %}

authorityInfoAccess = OCSP;URI:{{ pki_ocsp_url }}
{% endif %}

[ crl_ext ]
authorityKeyIdentifier = keyid:always
{% if pki_ocsp_url %}

[ authority_info_access ]
authorityInfoAccess = OCSP;URI:{{ pki_ocsp_url }}
{% endif %}

[ ocsp ]
basicConstraints = CA:FALSE
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid,issuer
keyUsage = critical, digitalSignature
extendedKeyUsage = critical, OCSPSigning
