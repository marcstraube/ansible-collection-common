# marcstraube.common.hardening/molecule/default/Vagrantfile
#
# MOLECULE_PLATFORM_NAME filters which VM to create.

platform = ENV['MOLECULE_PLATFORM_NAME'] || ''

Vagrant.configure("2") do |config|
  config.vm.box_check_update = false
  config.vm.synced_folder ".", "/vagrant", disabled: true

  if platform.empty? || platform == "hardening-archlinux"
    config.vm.define "hardening-archlinux" do |node|
      node.vm.box = "marcstraube/archlinux-ansible"
      node.vm.hostname = "hardening-archlinux"
      node.vm.network "forwarded_port", guest: 22, host: 2222, id: "ssh", auto_correct: false

      node.vm.provider "virtualbox" do |vb|
        vb.memory = "1024"
        vb.cpus = 1
        vb.name = "molecule-hardening-archlinux"
      end
    end
  end

  if platform.empty? || platform == "hardening-debian-trixie"
    config.vm.define "hardening-debian-trixie" do |node|
      node.vm.box = "cloud-image/debian-13"
      node.vm.hostname = "hardening-debian-trixie"
      node.vm.network "forwarded_port", guest: 22, host: 2223, id: "ssh", auto_correct: false

      node.vm.provider "virtualbox" do |vb|
        vb.memory = "1024"
        vb.cpus = 1
        vb.name = "molecule-hardening-debian-trixie"
      end
    end
  end

  if platform.empty? || platform == "hardening-rocky-9"
    config.vm.define "hardening-rocky-9" do |node|
      node.vm.box = "rockylinux/9"
      node.vm.hostname = "hardening-rocky-9"
      node.vm.network "forwarded_port", guest: 22, host: 2224, id: "ssh", auto_correct: false

      node.vm.provider "virtualbox" do |vb|
        vb.memory = "1024"
        vb.cpus = 1
        vb.name = "molecule-hardening-rocky-9"
      end
    end
  end

  if platform.empty? || platform == "hardening-rocky-10"
    config.vm.define "hardening-rocky-10" do |node|
      node.vm.box = "bento/rockylinux-10"
      node.vm.hostname = "hardening-rocky-10"
      node.vm.network "forwarded_port", guest: 22, host: 2225, id: "ssh", auto_correct: false

      node.vm.provider "virtualbox" do |vb|
        vb.memory = "1024"
        vb.cpus = 1
        vb.name = "molecule-hardening-rocky-10"
      end
    end
  end
end
