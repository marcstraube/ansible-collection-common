---
#
# Service Control
#

# Enable/disable the entire role
python_enabled: true

# Install pipx for user-level Python tool management
python_pipx_enabled: true

# Additional OS packages to install (e.g., python3-dev, python3-lxml)
python_extra_packages: []
