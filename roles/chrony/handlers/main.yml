---
# =============================================================================
# Chrony Handlers
# =============================================================================

- name: Restart chrony
  ansible.builtin.systemd:
    name: '{{ __chrony_service_name }}'
    state: restarted
  when: chrony_enabled | bool
