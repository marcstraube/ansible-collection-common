---
- name: Prepare
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Ensure /etc/modprobe.d exists
      ansible.builtin.file:
        path: /etc/modprobe.d
        state: directory
        owner: root
        group: root
        mode: '0755'
