---
- name: Reload GPU udev rules
  ansible.builtin.command:
    cmd: udevadm control --reload-rules
  changed_when: false
  when: graphics_enabled | bool

- name: Trigger GPU udev rules
  ansible.builtin.command:
    cmd: udevadm trigger
  changed_when: false
  when: graphics_enabled | bool
