---
- name: Reload graphics udev rules
  ansible.builtin.command:
    cmd: udevadm control --reload-rules
  changed_when: true

- name: Trigger udev rules
  ansible.builtin.command:
    cmd: udevadm trigger
  changed_when: true
